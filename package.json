{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=8000",
    "start": "concurrently \"yarn run dev\" \"yarn run json\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "json": "json-server --watch db.json --port 3004 --no-cors,"

  },
  "devDependencies": {
    "nuxt": "3.0.0-rc.6"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.6",
    "@pinia/nuxt": "^0.3.1",
    "concurrently": "^7.3.0",
    "json-server": "^0.17.0",
    "pinia": "^2.0.17",
    "sass": "^1.54.0",
    "typescript": "^4.7.4",
    "vue-toastification": "^2.0.0-rc.5"
  }
}
